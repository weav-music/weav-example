package io.weav.example.ui.run

class SupportingFileInfo {
    companion object {
        val songBundle = "WeavExampleEmbeddedSongs.bundle"
        val playlistId= "io.weav.bundle.WeavExample"
        val workoutIdentifier = "kelly_roberts_400m_repeats"
        val workoutPath = workoutIdentifier + ".enc"
        val workoutBundlePath = workoutIdentifier + ".bundle"
        val instructorBundleIdentifier = "kelly_roberts_instructor_embedded"
        val instructorBundlePath = instructorBundleIdentifier + ".bundle"
    }
}